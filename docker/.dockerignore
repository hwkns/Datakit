# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artifacts (we only want the dist folder)
.git/
.gitignore
README.md
package.json
package-lock.json
yarn.lock
src/
public/
.env*

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
.sublime-*

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs/
*.log

# Testing
coverage/
.nyc_output/
.jest/

# Temporary files
tmp/
temp/
*.tmp

# Documentation (not needed in container)
docs/
*.md
!README.md

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml
.circleci/

# Docker files (avoid recursion)
Dockerfile*
docker-compose*
.dockerignore

# Other development files
.eslintrc*
.prettierrc*
tsconfig.json
webpack.config.js
vite.config.js

# Keep only these essential files:
# dist/ (our built React app)
# nginx.conf (our nginx configuration)
# Dockerfile (needed for build)